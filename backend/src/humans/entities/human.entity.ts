export class Human {
  private lastName: string;
  private firstName: string;
  private vat: string;

  constructor(lastName: string, firstName: string, vat: string) {
    this.lastName = lastName;
    this.firstName = firstName;
    this.vat = vat;
  }
}
